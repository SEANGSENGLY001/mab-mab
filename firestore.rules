rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access, restrict writes to authenticated users only
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
